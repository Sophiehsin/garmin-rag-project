# Garmin RAG Project — Python Dependencies

# Web Framework
fastapi
uvicorn[standard]
python-multipart          # file upload support

# Database & ORM
sqlmodel
sqlalchemy
psycopg2-binary           # PostgreSQL driver (psycopg2)
psycopg[binary]           # psycopg3 driver required by langchain-postgres

# Auth (Task 4)
python-jose[cryptography] # JWT encoding/decoding
httpx                     # async HTTP client (Google OAuth token exchange + tests)

# LLM & RAG
langchain-core
langchain-postgres        # PGVector vector store
langchain-huggingface     # HuggingFaceEmbeddings wrapper
langchain-google-genai    # Gemini LLM integration

# Embedding & Reranking (Tasks 1–9)
sentence-transformers     # all-MiniLM-L6-v2 embedder + ms-marco cross-encoder reranker

# Settings
pydantic-settings

# Evaluation (Task 6)
ragas                     # RAG evaluation framework

# Testing
pytest
pytest-asyncio
